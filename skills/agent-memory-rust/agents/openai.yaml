version: 1
display_name: Agent Memory Rust
short_description: Build and harden Rust append-only versioned memory graph engine
default_prompt: Implement or optimize the Rust memory engine with strict append-only DAG invariants, project-local storage under .M2W/memory, and production CLI/runtime usage via --project and --serve.
