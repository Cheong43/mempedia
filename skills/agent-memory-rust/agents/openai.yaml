version: 1
display_name: Agent Memory Rust
short_description: Build and harden Rust append-only versioned memory graph engine
default_prompt: Implement or optimize the Rust memory engine with strict append-only DAG invariants, project-local storage under .M2W/memory, wiki-style Markdown body content, keyword fuzzy search, budgeted multi-hop exploration (explore_with_budget), and access-driven importance updates for production CLI/runtime usage via --project and --serve.
